services:
  db:
    image: postgres:16
    environment:
      POSTGRES_PASSWORD: postgres
      POSTGRES_USER: postgres
      POSTGRES_DB: bodetech
    ports:
      - "5432:5432"
    volumes:
      - bodetech_pg:/var/lib/postgresql/data
volumes:
  bodetech_pg:
